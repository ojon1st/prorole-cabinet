header.topbar.navbar.navbar-inverse.navbar-fixed-top.inner
  // start: TOPBAR CONTAINER
  .container
    .navbar-header
      if (message)
        | #{message}
      a.sb-toggle-left.hidden-md.hidden-lg(href='#main-navbar')
        i.fa.fa-bars
      // start: LOGO
      
      // end: LOGO
    .topbar-tools
      // start: TOP NAVIGATION MENU
      ul.nav.navbar-right
        // start: USER DROPDOWN
        li.current-user
          a.dropdown-toggle(href='/logout')
            if (cabinet)
              span.strong.username #{cabinet.denomination}
        // end: USER DROPDOWN
      // end: TOP NAVIGATION MENU
      // end: TOPBAR CONTAINER
